# Local environment template for Chapp
# ------------------------------------------------------------
# How to use:
# 1) Copy this file to ".env.local" for local development.
#    Example: cp local.env.example .env.local
# 2) Fill in the placeholders below. Do NOT commit your .env.local.

# ------------------------------------------------------------
# App / Dev utilities
# ------------------------------------------------------------
# Base URL used by debug/test scripts. Usually your local dev server.
BASE_URL=http://localhost:3000

# Activity logging behavior (server-side only)
# - ACTIVITY_LOG_TRANSPORT: db | console   (default: db)
# - ACTIVITY_LOG_DEBUG: true | false       (extra OK lines; default: false)
ACTIVITY_LOG_TRANSPORT=db
ACTIVITY_LOG_DEBUG=false

# ------------------------------------------------------------
# Supabase (Project Settings → API)
# ------------------------------------------------------------
# Public values used by the browser. Safe to expose to the client.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only secrets. DO NOT expose to client.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# ------------------------------------------------------------
# Database (Supabase → Database → Connection string)
# ------------------------------------------------------------
# DATABASE_URL: Pooled connection (pgbouncer; usually port 6543)
# Recommended flags: ?pgbouncer=true&connection_limit=1
DATABASE_URL=

# DIRECT_URL: Direct connection (non-pooled; usually port 5432)
DIRECT_URL=

# SHADOW_DATABASE_URL: Optional, used by Prisma Migrate for shadow schema
# If not provided, we can reuse DIRECT_URL during setup.
SHADOW_DATABASE_URL=

# ------------------------------------------------------------
# OAuth Providers
# ------------------------------------------------------------
# Google (Google Cloud Console → APIs & Services → Credentials)
# Authorized redirect URI:
#   https://<project-ref>.supabase.co/auth/v1/callback
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# GitHub (GitHub → Settings → Developer settings → OAuth Apps)
# Authorization callback URL:
#   https://<project-ref>.supabase.co/auth/v1/callback
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# ------------------------------------------------------------
# Admin / bootstrap
# ------------------------------------------------------------
# Comma-separated list of emails to flag as admins during setup/migrations.
INITIAL_ADMIN_EMAILS=


